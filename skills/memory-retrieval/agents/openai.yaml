interface:
  display_name: "Memory Retrieval"
  short_description: "Search and retrieve records from the standalone memory tool"
  default_prompt: "Use the memory_tool CLI to search, timeline, and get observations from the memory database."
