# Archive

This directory holds historical material that should not be treated as the current product contract.

Examples:
- child-session notes
- timestamped implementation summaries
- superseded integration writeups

Use `docs/current/` and `README.md` as the source of truth for current behavior.
